{
  "deploy": {
    "startCommand": "(php artisan migrate --force || true) && (php artisan schedule:work &) && php -S 0.0.0.0:$PORT -t public",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}